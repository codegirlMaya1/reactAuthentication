import React from 'react';
import { saveToken, getToken, removeToken, saveUserCredentials, getUserCredentials } from './TokenManager';

const SessionStorageManager: React.FC = () => {
  return null; // This component doesn't need to render anything.
};

export default SessionStorageManager;
export { saveToken, getToken, removeToken, saveUserCredentials, getUserCredentials };
